#!/bin/bash 
TODAY=$(date)
HOST=$(hostname)
# updating wordpress to minor version
echo '<---------            $TODAY  at  $HOST          -------->'
wp core check-update --path=/var/www/html/ --minor --skip-themes=all


